# --- secrets & machine state: never commit ---
.env
podenv/id_ed25519
podenv/id_ed25519.pub
podenv/.pod_state.json

# --- python noise ---
__pycache__/
*.pyc
.venv/

# --- personal learning reports (not part of the evidence chain) ---
report/

# --- intermediate training checkpoints (~75 MB); the final adapter IS committed ---
checkpoints/qwen05b-lora/checkpoint-*/

# --- blog drafts & publishing material (local only) ---
substack/

# --- local session context for AI assistants ---
CLAUDE.md
AGENTS.md

# NOTE: trace/ (CPU trace tools + dumps) is deliberately NOT ignored —
# the machine post promises it lands in this repo; commit is pending.
